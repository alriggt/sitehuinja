import { createMuiTheme } from "@mui/material"

const theme = createMuiTheme({
	palette: {
		primary: {
			main: "#3A68B8",
			dark: "#3A68B8",
			
		},
		secondary: {
			main: "#3A68B8",
		},
	},
	shape: {
		borderRadius: 15,
	},
});

export default theme;